# Phase 12G - UI Fixture Loader / Generated Export Preview Report

## Verdict

```text
Phase 12G UI Fixture Loader / Generated Export Preview: PASS
```

## Objective

Allow the local React/Vite UI to preview page-model JSON exports generated by
the Python workflow, while remaining a static read-only frontend.

## Files Created

```text
docs/PHASE12G_UI_FIXTURE_LOADER_CONTRACT.md
docs/PHASE12G_UI_FIXTURE_LOADER_REPORT.md
ui/public/page-models/saved-analysis-list.json
ui/public/page-models/saved-analysis-detail.json
ui/src/data/pageModelLoader.ts
```

## Files Modified

```text
ui/src/App.tsx
ui/src/styles.css
ui/src/types/userWorkflow.ts
docs/CODEX_CONTINUITY_HANDOFF.md
docs/NEXT_PHASE_CONTRACT.md
```

## Public Functions / Types Added

```text
PageModelExport
PageModelLoadResult
loadPageModelExport(...)
validatePageModelExport(...)
```

## Schema Impact

None.

## Work Completed

- Added static exported page-model samples under `ui/public/page-models/`.
- Added runtime loader for Phase 12F export envelopes.
- Added validation for page model version, export metadata, and core page model
  shape.
- Updated the UI to load generated list/detail exports by default.
- Added query parameter overrides:
  - `?listModel=/page-models/saved-analysis-list.json`
  - `?detailModel=/page-models/saved-analysis-detail.json`
- Preserved fixture fallback behavior if loading or validation fails.
- Added visible source/status metadata for loaded or fallback data.

## Validation Performed

Frontend build:

```text
> codie-ui@0.1.0 build
> tsc --noEmit && vite build

36 modules transformed.
dist/index.html                 0.41 kB
dist/assets/index-B1kzUvjH.css  3.03 kB
dist/assets/index-BQRhkJmF.js   203.12 kB
built in 529ms
```

Full Python test suite:

```text
Ran 290 tests in 0.791s

OK
```

Static checks:

```text
git diff --check
```

passed.

Boundary scans:

```text
rg -n "sqlite3|source_events|source_decks|provider_objects|codie\.providers|codie\.db|codie\.recommendations|codie\.analytics" ui\src ui\public
rg -n "should play|must include|correct card|breaks the format|secretly optimal|cut this" ui\src ui\public
```

both returned no matches.

## Boundary Notes

- UI uses browser `fetch(...)` against static JSON only.
- UI does not import Codie Python modules.
- UI does not read SQLite.
- UI does not call providers, analytics, or recommendations.
- No recommendation or strategic claim language was added.

## Known Caveats

- Generated JSON still needs to be copied or emitted into the static
  `ui/public/page-models/` path for preview.
- No local API server exists.
- No mobile sharing surface exists yet.

## Recommended Next Step

```text
Phase 12H - Local Report Share Bundle
```

Purpose:

Create a low-priority but useful export bundle for phone viewing, such as a
static HTML/PDF-ready report folder and QR-code-ready local path guidance,
without adding a hosted service or changing recommendation logic.

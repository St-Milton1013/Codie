# Architecture Decision Records

This directory records durable architecture decisions that future phases should not rediscover from long checkpoint history.

Use one ADR per decision. Keep each ADR short:

```text
Status
Context
Decision
Consequences
Validation
```

ADR files are governance documents. They do not authorize implementation scope by themselves.

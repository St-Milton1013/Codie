# Phase44T Read-Only Decision Core Contract Report

## Scope

Phase44T is documentation-only. It defines the bounded, pure Phase44U
Decision Core implementation and updates only continuation indexes.

## Planned Surface

Phase44U may accept caller-supplied bounded evidence and immutable accepted
Goal Engine records, then return `HEALTHY_IDLE` or an advisory Goal Candidate
with a draft Goal Contract. It must expose necessity, evidence, root cause,
history, actionability, experiment need, intervention, impact, and priority
rationale separately and preserve conflicts and limitations.

## Non-Authority Boundary

No real Goal, work order, selected task, approval, execution, persistence,
source fetch, model call, Stage 1 capability, UI/CLI, or Stream Deck surface is
authorized. Existing human-governed planning remains canonical.

## Next Gate

Phase44U is blocked pending independent validation and human merge of this
exact Phase44T packet.
